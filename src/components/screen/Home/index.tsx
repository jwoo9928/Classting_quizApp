import * as React from 'react';
import {
	View,
	StyleSheet,
} from 'react-native';


interface HomeProps {
	navigation?: any;
}

const Home = ({ navigation }: HomeProps) => {

	return (
		<View>

        </View>
	);
};

export default Home;

const styles = StyleSheet.create({

});
